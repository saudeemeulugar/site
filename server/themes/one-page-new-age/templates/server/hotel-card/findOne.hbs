<ol class="breadcrumb">
  <li><a href="/">Início</a></li>
  <li><a href="/hotel-card">{{t 'hotel-card.find'}}</a></li>
  <li class="active">Ficha #{{id}}</li>
</ol>

<h1>Ficha de registro #{{id}}</h1>

<table class="table table-striped table-hover">
  <tbody>
    {{#each data.dataValues }}
      <tr>
        <th>{{@key}}</th>
        <td>{{this}}</td>
      </tr>
    {{/each}}
  </tbody>
  <tfoot>
    <td colspan="2">
      {{we-action-edit-btn resourceName data req data.id}}
      {{we-action-delete-btn resourceName data req data.id}}
    </td>
  </tfoot>
</table>